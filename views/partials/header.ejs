<nav class="navbar navbar-expand-lg navbar-light bg-light rounded">
  <a class="navbar-brand" href="/">Merit</a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <!-- Loopa igenom alla entries och lägg till varje nyckel i headern -->
      <% for (let endpoint in endpoints) { %>
        <li class="nav-item">
          <a class="nav-link" href=<%= endpoints[endpoint]%>> <%=endpoint%> <span class="sr-only">(current)</span></a>
        </li>
      <% } %>
    </ul>

    <% if (typeof inloggad !== 'undefined') { %>
      <form class="form-inline my-2 my-lg-0">
        <input class="form-control mr-sm-2" type="search" placeholder="Sök" aria-label="Search">
      </form>
      <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i></a>
    <% }  %>

  </div>
</nav>